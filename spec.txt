



When dragging, first we should of course check if the mouse is in the collision area for a tile, Then we should check the line from the last selected tile to the current position, and if that intersects a tile, we should. Should select that tile, selecting the one closest to the last selected tile. That way, if they select really quickly and they jump from one tile all the way across the board, we can select the intermediate tiles, as it's very possible for them to move their mouse this fast, moving across the entire board within one frame. 







Review security

Ask the AI to review security

use an always-up wrapper

- Have the server host the files
    - And build them?

Deploy to quentinbrooks.com

Make custom domain for it with cloudflare





Better game lobby display
    - When we finish, we show too much information. We show the list of players, but we also show the final scores. Those are redundant. We should be using the same UI for it and just have the score, the ranking, and the words only show if we have that information. 

In single player auto start if they don't do anything after 5 seconds

Do some testing with a lot of words found



Mobile support
    ensure touch support works
    tactile feedback
    orientation support (always orient in the same way)

Fix the single player game over screen on mobile
    - Make a universal game over screen that can show the score anywhere we need it (multiplayer, etc), and have this show as a page instead of some kind of weird pop-up. 

- Smaller dictionary
    - Allow picking between different dictionaries
        - Zip and unzip them?
    - Download the dictionaries on load, with progress.

- Better UI

- Make it so that start click area is larger for the first letter.

- Sounds

- Saved state, so you can refresh and not break a multiplayer game

- History mode, which will let you view the finish screen for old games 
    - Change the finish screen so it's inline, instead of using a popup

- Penalty for wrong words